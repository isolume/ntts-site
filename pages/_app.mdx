import "../styles.css";
import PlausibleProvider from 'next-plausible'

export default function Nextra({ Component, pageProps }) {
  return (
    <>
      <Component {...pageProps} />
      <PlausibleProvider domain="notexttospeech.com" customDomain="https://think.tsukim.io" selfHosted="true" />
    </>
  );
}
