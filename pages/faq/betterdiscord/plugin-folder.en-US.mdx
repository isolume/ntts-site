import { Tabs, Tab, Steps } from 'nextra-theme-docs'
import Link from 'next/link'

<Tabs items={['Windows', 'macOS', 'Linux']}>
    <Tab>
        <Steps>
            ### Open Run

            Press  `⊞ Win + R`.

            ### Enter folder

            Paste in `%appdata%/betterdiscord/` and press `Enter ↩`.
        </Steps>
    </Tab>
    <Tab>
        <Steps>
            ### Open Go To dialogue

            Open finder, and then press `⌘ Command + ⇧ Shift + G`.

            ### Go to folder

            Paste in `~/Library/ApplicationSupport/betterdiscord/` and press  `return ↩`.
        </Steps>
    </Tab>
    <Tab>
        Navigate to `~/.config/BetterDiscord/`.
    </Tab>
</Tabs>
