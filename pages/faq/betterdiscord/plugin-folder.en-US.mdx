import { Callout, Tabs, Tab } from 'nextra-theme-docs'
import Link from 'next/link'

<Tabs items={['Windows', 'macOS', 'Linux']}>
    <Tab>
        1. Press `Press ⊞ Win + R`.
        2. Paste `%appdata%/betterdiscord/` and press `Enter ↩`.
    </Tab>
    <Tab>
        1. Open Finder.
        2. Press `⇧ Shift + ⌘ Command + G`.
        3. Paste: `~/Library/ApplicationSupport/betterdiscord/` and press  `return ↩`.
    </Tab>
    <Tab>
        1. Navigate to `~/.config/BetterDiscord/`.
    </Tab>
</Tabs>
